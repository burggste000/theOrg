Project Tasks:
    -Ask Dad why he didn't change managerId to manager_id in api.js on line 195?
    -Ask Dad the best way to make a fifth element to the CSV file be an empty string('')?
    -The add button isn't working at all.
    -Upload doesn't reload doesn't work after deleting an employee unless the page has been refreshed.
Tasks:
    -How can I use reset.css in React?
    -Make all components know the JavaScript functions in my old project's index.js file.
    -Ask Dad if I should have a smaller components that will be rendered in the add component for all of the input fields that get re-rendered in the forms.